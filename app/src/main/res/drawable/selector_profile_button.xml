<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state (when Home is selected) -->
    <item android:drawable="@drawable/profile_icon_selected" android:state_checked="true" />

    <!-- Unselected state (when Home is not selected) -->
    <item android:drawable="@drawable/profile_icon_unselected" />
</selector>
